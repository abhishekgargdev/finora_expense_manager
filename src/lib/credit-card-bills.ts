export function serializeBill(item: { _id: { toString(): string }; billingMonth: string; totalAmount: number; dueDate: Date; isPaid: boolean; paidDate?: Date | null; paidAmount?: number }) { return { id: item._id.toString(), billingMonth: item.billingMonth, totalAmount: item.totalAmount, dueDate: item.dueDate.toISOString(), isPaid: item.isPaid, paidDate: item.paidDate?.toISOString() ?? null, paidAmount: item.paidAmount }; }
