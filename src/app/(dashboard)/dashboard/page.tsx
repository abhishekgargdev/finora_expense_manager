"use client";
import * as React from "react";
import Link from "next/link";
import { Cell, Bar, BarChart, Line, LineChart, Pie, PieChart, XAxis, YAxis } from "recharts";
import { ArrowDownRight, ArrowUpRight, Banknote, CreditCard, HandCoins, Landmark, PiggyBank, ReceiptText, TrendingUp, WalletCards } from "lucide-react";
import MoneyText from "@/components/finance/MoneyText";
import StatCard from "@/components/finance/StatCard";
import PageSkeleton from "@/components/loader/PageSkeleton";
import { ChartContainer, ChartTooltip, ChartTooltipContent, type ChartConfig } from "@/components/ui/chart";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
type Summary = any;
const colors = ["var(--income)", "var(--expense)", "var(--investment)", "var(--primary)", "var(--pending)", "#8b5cf6"];
const config = { income: { label: "Income", color: "var(--income)" }, expense: { label: "Expense", color: "var(--expense)" }, value: { label: "Value", color: "var(--investment)" } } satisfies ChartConfig;
export default function DashboardPage() { const [data, setData] = React.useState<Summary | null>(null); const [month, setMonth] = React.useState("all"); const [year, setYear] = React.useState(String(new Date().getFullYear())); React.useEffect(() => { void (async () => { const params = new URLSearchParams(month === "all" ? {} : { month, year }); const response = await fetch(`/api/dashboard/summary?${params}`); if (response.ok) setData(await response.json()); })(); }, [month, year]); if (!data) return <PageSkeleton variant="chart" />; const distribution = Object.entries(data.investments.distribution).map(([name, value]) => ({ name, value })); const cards = [[<Landmark />, "Total Bank Balance", data.bankBalance], [<ArrowUpRight />, "Total Income", data.period.income], [<ArrowDownRight />, "Total Expense", data.period.expense], [<PiggyBank />, "Net Savings", data.period.netSavings], [<TrendingUp />, "Total Investments", data.investments.value], [<HandCoins />, "Pending to Receive", data.lending.pendingGiven], [<CreditCard />, "Credit Card Outstanding", data.creditOutstanding]] as const; return <div className="space-y-6"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><h2 className="font-heading text-2xl font-semibold">Financial overview</h2><p className="mt-1 text-sm text-muted-foreground">Your money, spending, and financial position at a glance.</p></div><div className="flex gap-2"><Select value={month} onValueChange={(value) => setMonth(value ?? "all")}><SelectTrigger className="w-32"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="all">All time</SelectItem>{Array.from({ length: 12 }, (_, index) => <SelectItem key={index + 1} value={String(index + 1)}>{new Date(2026, index, 1).toLocaleString("default", { month: "short" })}</SelectItem>)}</SelectContent></Select><Select value={year} onValueChange={(value) => setYear(value ?? String(new Date().getFullYear()))}><SelectTrigger className="w-24"><SelectValue /></SelectTrigger><SelectContent>{[0, 1, 2].map((offset) => <SelectItem key={offset} value={String(new Date().getFullYear() - offset)}>{new Date().getFullYear() - offset}</SelectItem>)}</SelectContent></Select></div></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([icon, label, value]) => <StatCard key={label} icon={icon} label={label} value={value} />)}</div><div className="grid gap-5 xl:grid-cols-[1.55fr_1fr]"><section className="card p-5"><h3 className="font-heading font-semibold">Income vs Expense</h3><p className="text-sm text-muted-foreground">Last 12 months</p><ChartContainer config={config} className="mt-4 h-72 w-full"><LineChart data={data.trend}><XAxis dataKey="month" tickFormatter={(value) => value.slice(5)} /><YAxis /><ChartTooltip content={<ChartTooltipContent />} /><Line dataKey="income" stroke="var(--color-income)" strokeWidth={3} dot={false} /><Line dataKey="expense" stroke="var(--color-expense)" strokeWidth={3} dot={false} /></LineChart></ChartContainer></section><section className="card p-5"><h3 className="font-heading font-semibold">Expense by category</h3><ChartContainer config={config} className="mt-4 h-72 w-full"><PieChart><ChartTooltip content={<ChartTooltipContent nameKey="name" />} /><Pie data={data.expenseCategories} dataKey="value" nameKey="name" innerRadius="55%" outerRadius="82%">{data.expenseCategories.map((_: unknown, index: number) => <Cell key={index} fill={colors[index % colors.length]} />)}</Pie></PieChart></ChartContainer></section></div><div className="grid gap-5 xl:grid-cols-3"><section className="card p-5 xl:col-span-2"><h3 className="font-heading font-semibold">Investment distribution</h3><ChartContainer config={config} className="mt-4 h-64 w-full"><BarChart data={distribution}><XAxis dataKey="name" /><YAxis /><ChartTooltip content={<ChartTooltipContent />} /><Bar dataKey="value" fill="var(--investment)" radius={[5, 5, 0, 0]} /></BarChart></ChartContainer></section><section className="card p-5"><h3 className="font-heading font-semibold">Bank balances</h3><div className="mt-4 grid gap-3">{data.accounts.map((account: any) => <div key={account.id} className="rounded-xl p-3 text-white" style={{ background: `linear-gradient(135deg, ${account.themeColor || "#1e3a5f"}, #111827)` }}><div className="text-xs text-white/70">{account.bankName} · •••• {account.last4Digits || "----"}</div><div className="mt-1 text-sm font-medium">{account.name}</div><MoneyText value={account.currentBalance} className="mt-1 text-xl font-bold text-white" /></div>)}</div></section></div><div className="grid gap-5 lg:grid-cols-3"><People title="People who owe me" items={data.lending.owedToMe} /><People title="People I owe" items={data.lending.iOwe} /><section className="card p-5"><h3 className="font-heading font-semibold">Recent transactions</h3><div className="mt-3 space-y-3">{data.recent.map((item: any, index: number) => <div key={index} className="flex items-center gap-3"><span className={`flex size-8 items-center justify-center rounded-full ${item.type === "Income" ? "bg-income-10 text-income" : item.type === "Expense" ? "bg-expense-10 text-expense" : "bg-primary/10 text-primary"}`}>{item.type === "Income" ? <ArrowUpRight className="size-4" /> : <ReceiptText className="size-4" />}</span><div className="min-w-0 flex-1"><div className="truncate text-sm font-medium">{item.title}</div><div className="text-xs text-muted-foreground">{item.type}</div></div><MoneyText value={item.amount} variant={item.type === "Income" ? "positive" : "negative"} /></div>)}</div></section></div></div>; }
function People({ title, items }: { title: string; items: { person: string; pending: number }[] }) { return <section className="card p-5"><div className="flex items-center justify-between"><h3 className="font-heading font-semibold">{title}</h3><Link href="/lending" className="text-xs text-primary hover:underline">View all</Link></div><div className="mt-3 space-y-3">{items.length ? items.map((item) => <div key={item.person} className="flex items-center justify-between"><span className="text-sm font-medium">{item.person}</span><MoneyText value={item.pending} /></div>) : <p className="text-sm text-muted-foreground">Nothing pending.</p>}</div></section>; }
