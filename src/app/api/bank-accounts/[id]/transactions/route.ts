import { NextRequest, NextResponse } from "next/server";
import { getUserId } from "../../route";
import connect from "@/lib/db";
import BankAccountModel from "@/models/BankAccount";
import BankTransactionModel from "@/models/BankTransaction";
export async function GET(_: NextRequest, context: RouteContext<"/api/bank-accounts/[id]/transactions">) { try { await connect(); const userId = await getUserId(); const { id } = await context.params; if (!await BankAccountModel.exists({ _id: id, user: userId })) return NextResponse.json({ error: "Bank account not found." }, { status: 404 }); const transactions = await BankTransactionModel.find({ user: userId, bankAccount: id }).sort({ date: -1, createdAt: -1 }).lean(); return NextResponse.json({ transactions: transactions.map((item) => ({ id: item._id.toString(), type: item.type, amount: item.amount, description: item.description, date: item.date.toISOString(), source: item.source, refId: item.refId?.toString() ?? null, balanceAfter: item.balanceAfter })) }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to load transactions." }, { status: 400 }); } }
export async function POST(request: NextRequest, context: RouteContext<"/api/bank-accounts/[id]/transactions">) { try { await connect(); const userId = await getUserId(); const { id } = await context.params; if (!await BankAccountModel.exists({ _id: id, user: userId })) return NextResponse.json({ error: "Bank account not found." }, { status: 404 }); const body = await request.json(); const type = body.type === "Credit" || body.type === "Debit" ? body.type : ""; const amount = Number(body.amount); const description = typeof body.description === "string" ? body.description.trim() : ""; const date = new Date(String(body.date)); if (!type) throw new Error("Choose a transaction type."); if (!Number.isFinite(amount) || amount <= 0) throw new Error("Amount must be greater than zero."); if (!description) throw new Error("Description is required."); if (Number.isNaN(date.getTime())) throw new Error("A valid date is required."); const transaction = await BankTransactionModel.recordTransaction({ user: userId, bankAccount: id, type, amount, description, date, source: "Manual" }); return NextResponse.json({ transaction: { id: transaction._id.toString(), type: transaction.type, amount: transaction.amount, description: transaction.description, date: transaction.date.toISOString(), source: transaction.source, balanceAfter: transaction.balanceAfter } }, { status: 201 }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Unable to add transaction." }, { status: 400 }); } }
